<% if current_user.username != nil  %>
  <h1> Welcome, <%= current_user.username %></h1>
<% end %>

<h4>See what features people are fantasising about!</h4>

<% @features.each do |f| %>
  <div>
    <%= f.song %> - <%= f.artist1 %> ft. <%= f.artist2 %>
  </div>
<% end %> 

<a href="/logout">Log Out</a>
<a href="/features/new">Create Another Feature</a>
<a href="/features/user/<%=current_user.id %>">View Feature</a>

